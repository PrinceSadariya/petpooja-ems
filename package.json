{
	"name": "petpooja-ems",
	"version": "1.0.0",
	"description": "",
	"main": "index.js",
	"scripts": {
		"test": "echo \"Error: no test specified\" && exit 1",
		"i-both": "concurrently \"cd server && npm i\" \"cd client && npm i\"",
		"dev": "concurrently \"cd server && npx nodemon index.js\" \"cd client && npm run dev\""
	},
	"author": "",
	"license": "ISC",
	"devDependencies": {
		"concurrently": "^9.1.2"
	}
}
